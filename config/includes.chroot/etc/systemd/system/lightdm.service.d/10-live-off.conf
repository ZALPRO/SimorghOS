# SimorghOS — LightDM runs on installed systems; on live media it is
# skipped (condition fails) so the boot path goes straight to the Hyprland
# session. No runtime masking / daemon-reload needed.
[Unit]
ConditionKernelCommandLine=!boot=live
