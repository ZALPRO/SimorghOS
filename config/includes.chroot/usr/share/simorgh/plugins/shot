#!/usr/bin/env bash
# SimorghOS plugin — quick screenshot (area) to clipboard
set -euo pipefail
grim -g "$(slurp)" - | wl-copy
notify-send "Simorgh" "Screenshot copied to clipboard" 2>/dev/null || true
echo "area screenshot → clipboard"
